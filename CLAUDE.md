# 프로젝트 개요

책과 관련된 링크를 모아두는 링크트리 스타일의 웹사이트.

## 목적

독자들이 책 관련 추천 콘텐츠, 서점, 리뷰, 커뮤니티 등 유용한 링크를 한 곳에서 찾을 수 있도록 모아두는 단일 페이지 웹앱.

## 기술 스택

- 순수 HTML / CSS / JavaScript (프레임워크 없음)
- 별도 빌드 도구 없음 — 파일을 직접 브라우저에서 열 수 있어야 함

## 디자인 시스템

### 색상 팔레트

| 용도 | 라이트 모드 | 다크 모드 |
|------|------------|----------|
| 배경 | `#ffffff` | `#121212` |
| 주요 텍스트 | `#000000` | `#ffffff` |
| 포인트 (버튼, 링크 강조) | `#e85811` | `#e85811` |
| 보조 색상 (헤더, 아이콘) | `#363090` | `#363090` |
| 카드 배경 | `#f5f5f5` | `#1e1e1e` |
| 카드 테두리 | `#e0e0e0` | `#2a2a2a` |

**규칙:** 위 5가지 색상(`#ffffff`, `#000000`, `#e85811`, `#363090`, 카드 배경용 회색 계열) 외의 색은 사용하지 않는다.

### 타이포그래피

- 시스템 폰트 스택 사용: `-apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif`
- 한글 폰트: `'Noto Sans KR'` (Google Fonts CDN)

### 다크 모드

- CSS `prefers-color-scheme: dark` 미디어 쿼리로 자동 전환
- 토글 버튼으로 수동 전환도 지원 (`localStorage`에 설정 저장)

## 레이아웃 & 반응형

- **모바일 우선** 설계 — 기준 너비 `390px` (iPhone 14)
- 최대 너비 `480px`, 중앙 정렬
- 터치 타겟 최소 높이 `48px`
- 링크 카드는 세로로 쌓이는 단일 컬럼

## 컴포넌트

### 프로필 섹션 (상단)
- 아바타 이미지 (원형, 80×80px)
- 사이트 제목 (h1)
- 짧은 소개 문구

### 링크 카드
- 아이콘 + 링크 이름 + 짧은 설명
- `hover` / `active` 상태: 포인트 색(`#e85811`) 테두리 + 살짝 올라오는 효과(`transform: translateY(-2px)`)
- `target="_blank" rel="noopener noreferrer"` 필수

### 카테고리 구분
- 링크 그룹은 카테고리 레이블로 구분
- 레이블 색상: `#363090`

### 다크 모드 토글
- 우상단 고정
- 아이콘: 달 / 해 SVG

## 파일 구조

```
baro_claude_linktree/
├── index.html       # 단일 진입점
├── style.css        # 전체 스타일
├── script.js        # 다크 모드 토글, 인터랙션
└── CLAUDE.md
```

## 코딩 규칙

- 외부 의존성은 Google Fonts CDN만 허용
- JavaScript는 vanilla만 사용, 번들러 금지
- CSS 변수(`--color-*`)로 색상 관리하여 다크 모드 전환을 단일 지점에서 처리
- 접근성: `aria-label`, 충분한 색 대비(WCAG AA 이상)
- 링크 카드에 애니메이션은 `prefers-reduced-motion` 미디어 쿼리로 비활성화 지원
